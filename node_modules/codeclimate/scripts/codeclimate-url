#!/usr/bin/env bash
{ set +x; } 2>/dev/null

fullname="$(github-remote-fullname)" || exit
[[ -z $fullname ]] && echo "ERROR: 0 github remotes" && exit 0

url="https://codeclimate.com/github/$fullname"
echo "$url"
