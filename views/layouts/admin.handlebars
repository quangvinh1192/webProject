<!DOCTYPE html>
<html>
<head>
    <title>{{ title }}</title>
    <link rel="shortcut icon" href="/images/favicon.ico" />
    {{>styles}}

</head>
<body class="skin-blue">
<div class="wrapper">
    {{>header}}

    {{>sidebar-admin}}
    <div class="content-wrapper">
        {{{body}}}
    </div>

    {{>footer}}
    {{>scripts}}
</div>

</body>
</html>
